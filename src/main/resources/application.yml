spring:
  datasource:
    #    initialization-mode: always
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/{데이터베이스이름입력}?serverTimezone=Asia/Seoul
    username: {데이터베이스 아이디}
    password: {데이터베이스 비밀번호}

  # JPA Configuration
  jpa:
    defer-datasource-initialization: true
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
